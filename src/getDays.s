# module for getDays(char *buffer, int mon, int year)

    .global getDays
    
    .data

    .text
# TODO - put strings here

getDays:
    push %rbp
    mov %rsp, %rbp

    # TODO
    
    pop %rbp
    ret
