# module for getDaysOfWeek(char *buffer);

    .global getDaysOfWeek
    
    .data

    .text
# TODO - put strings here

getDaysOfWeek:
    push %rbp
    mov %rsp, %rbp

    # TODO
    
    pop %rbp
    ret
