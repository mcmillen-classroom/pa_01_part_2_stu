# module for getMonthYear(char *buffer, int mon, int year)

    .global getMonthYear
    .type getMonthYear, @function
    
    .data

    .text
# TODO - put strings here

getMonthYear:
    push %rbp
    mov %rsp, %rbp

    # TODO
    
    pop %rbp
    ret
